# Whisper Bundle Layout

Optional alternate layout for whisper.cpp assets.

Expected model names:

- `ggml-base.bin`
- `ggml-small.bin`
- `ggml-medium.bin`
- `ggml-large-v3.bin`

This directory is copied into app resources as `whisper/models/`.
